# 作業日誌 - 2026-02-13

## 概述
完成 Smart Objects 課程本週作業：Fatigue Detection 和 Gaze Estimation 測試，並設定遠程訪問環境。

---

## 設備狀態

| Pi | 本地 IP | Tailscale IP | 狀態 |
|----|---------|--------------|------|
| Horizon | 10.1.65.75 | 100.101.140.92 | ✅ 正常 |
| Gravity | 10.1.65.74 | 100.81.200.91 | ✅ 正常 |
| Orbit | 10.1.70.252 | -- | 老師使用中 |

---

## 完成任務

### 1. 環境設定
- [x] Clone smart-objects-cameras repo 到本地
- [x] 設定 SSH 連接到 Gravity 和 Horizon
- [x] 測試攝影機連接（OAK-D MyriadX）

### 2. Fatigue Detection（基本要求）
- [x] 同步 `fatigue_detector.py` 到 Horizon
- [x] 測試運行成功
- [x] 偵測指標：眼睛閉合 (EAR) + 頭部傾斜 (Pitch)
- [x] 截圖保存：`latest_fatigue_frame.jpg`

### 3. Gaze Estimation（基本要求）
- [x] 同步 `gaze_detector.py` 到 Horizon
- [x] 測試運行成功
- [x] Pipeline：YuNet + Head Pose + Gaze ADAS
- [x] 截圖保存：`latest_gaze_frame.jpg`

### 4. Discord 通知（加分項）
- [x] 測試 Webhook 通知成功
- [x] Person Detector + Discord 整合測試
- [x] 通知格式：啟動/偵測/停止訊息

### 5. Tailscale 遠程訪問（額外）
- [x] Horizon 安裝 Tailscale (100.101.140.92)
- [x] Gravity 安裝 Tailscale (100.81.200.91)
- [x] 本機 Mac 安裝 Tailscale
- [x] 更新 SSH config 加入 `horizon-ts` 和 `gravity-ts`
- [x] 測試遠程連接成功

---

## 測試記錄

### Fatigue Detection 輸出
```
[2026-02-13 16:32:51] Detection started. Monitoring for fatigue...
Faces: 1 | Eyes: CLOSED (100%) | Head: TILTED (100%) | Fatigue: 100%
[2026-02-13 16:32:53] FATIGUE DETECTED (head tilted)
```

### Gaze Detection 輸出
```
[2026-02-13 16:59:11] Gaze detector started (DepthAI 3.x, YuNet + Head Pose + Gaze ADAS)
[2026-02-13 16:59:20] Detection started. Monitoring gaze direction...
Faces: 0 | Gaze: --
```

### Person Detection + Discord
```
[2026-02-13 16:41:40] Discord notifications: ENABLED
[2026-02-13 16:41:46] No person detected - area clear
```

---

## SSH 連接方式

```bash
# 本地網路（在學校）
ssh horizon      # 10.1.65.75
ssh gravity      # 10.1.65.74

# Tailscale 遠程（任何地方）
ssh horizon-ts   # 100.101.140.92
ssh gravity-ts   # 100.81.200.91
```

---

## 運行指令

```bash
# 啟用虛擬環境
activate-oak

# Fatigue Detection
python fatigue_detector.py --display --log

# Gaze Detection
python gaze_detector.py --display --log

# Person Detection + Discord
python person_detector.py --discord --log
```

---

## 檔案位置

### 本地 (Mac)
- Repo: `/Users/gc/Documents/GitHub/Smart_Object_Gordon/`
- 截圖: `~/Desktop/fatigue_frame_horizon.jpg`, `~/Desktop/gaze_frame_horizon.jpg`

### Pi (Horizon/Gravity)
- 專案: `~/oak-projects/`
- 日誌: `~/oak-projects/*_detection_*.log`
- 截圖: `~/oak-projects/latest_*_frame.jpg`

---

## 備註
- Gravity 有其他使用者 (seren) 在使用 person-detector.py
- 攝影機一次只能被一個程式使用
- Discord Webhook 已設定在 `.env` 檔案中

---

## 遠端即時串流測試

### SSH 免密碼設定
- [x] 透過 Tailscale 確認 Gravity 和 Horizon 在線
- [x] 用 `ssh-copy-id` 將本機 SSH key 部署到兩臺 Pi
- [x] 更新 `~/.ssh/config`，可直接 `ssh gravity` / `ssh horizon`

### OAK-D 拍照測試
- [x] 遠端拍照（兩臺同時），初次結果模糊
- [x] 啟用 `CONTINUOUS_VIDEO` 自動對焦 + 等待 60 幀穩定
- [x] 第二次拍照清晰度大幅提升
- 照片保存：`gravity_photo.jpg`、`horizon_photo.jpg`

### MJPEG 即時串流部署
- [x] 在兩臺 Pi 安裝 Flask（`sudo pip install flask`）
- [x] 部署 `camera_stream.py` 到 `~/oak-projects/`
- [x] 串流伺服器啟動成功（port 8080，15 FPS）

串流腳本要點：
- 使用 Flask + MJPEG（`multipart/x-mixed-replace`）
- depthai 3.x `ColorCamera` + `CONTINUOUS_VIDEO` 自動對焦
- 線程分離：相機捕獲 + HTTP 服務各自獨立

即時串流網址：

| 相機 | 網址 |
|------|------|
| Gravity | http://100.81.200.91:8080 |
| Horizon | http://100.101.140.92:8080 |

啟動方式：
```bash
ssh gravity "source /opt/oak-shared/venv/bin/activate && nohup python3 ~/oak-projects/camera_stream.py > /tmp/camera_stream.log 2>&1 &"
```

停止方式：
```bash
ssh gravity "pkill -f camera_stream.py"
```

---

## OAK-D 深度感測與空間掃描測試

### 深度圖擷取
- [x] 建立 stereo depth pipeline（MonoCamera CAM_B/CAM_C + StereoDepth）
- [x] 同步擷取 RGB + Depth（depth aligned to CAM_A）
- [x] 深度範圍 clip 到 0.3-5m，JET 色彩映射可視化
- [x] 生成 RGB + Depth 並排對比圖：`gravity_depth_combined.jpg`

API 筆記（depthai 3.x）：
- `MonoCamera` 用 `setBoardSocket(dai.CameraBoardSocket.CAM_B)` 而非 `setCamera('left')`
- `StereoDepth.PresetMode` 可用值：`DENSITY`, `ACCURACY`, `FAST_DENSITY` 等（無 `HIGH_DENSITY`）
- 相機佔用互斥：串流伺服器需先 `pkill` 才能跑其他 pipeline

### 3D 點雲生成
- [x] 讀取相機內參（`readCalibration` → fx=1483, fy=1482, cx=945, cy=522）
- [x] 深度像素反投影為 3D 點（`x = (u-cx)*z/fx`）
- [x] 輸出 PLY 格式（ASCII，含 RGB 顏色）
- [x] Gravity 點雲：348,546 點（`gravity_pointcloud.ply`）
- [x] Horizon 點雲：221,639 點（`horizon_pointcloud.ply`）

### 3D 可視化
- [x] 安裝 MeshLab（`brew install --cask meshlab`）— 打開但未顯示
- [x] Open3D — macOS GLFW 錯誤
- [x] 最終方案：Three.js 瀏覽器互動式點雲（`horizon_pointcloud.html`）
- [x] 支援深度著色 / RGB 著色切換、點大小滑桿

### 深度疊加可視化
- [x] 將 3D 點投射回 2D，疊在原始照片上（`horizon_depth_on_photo.jpg`）
- [x] 深度色彩：紅=近（0.3m）→ 藍=遠（5m）
- [x] 四格對比圖：RGB / Depth Overlay / Measurements / Legend（`horizon_comparison.jpg`）

### 三軸空間測量
- [x] 在照片上三個位置繪製 X/Y/Z 三軸標線（`horizon_3axis.jpg`）
  - Table (z=1.3m)：0.5m 軸
  - Mid (z=5.0m)：1.0m 軸
  - Back (z=5.0m)：1.0m 軸
- [x] 地面深度刻度尺（每 0.5m 標記）
- 近處 1m vs 遠處 1m 的透視壓縮效果清楚可見

### OAK-D 深度感測限制
| 參數 | 值 |
|------|------|
| 立體基線 | ~7.5cm |
| 最小距離 | ~20cm |
| 最大有效距離 | ~5-10m（精度隨距離² 衰減）|
| 水平視角 | ~69° |
| 垂直視角 | ~54° |
| 限制因素 | 基線短、固定鏡頭、需要紋理、單一視角 |

### 生成的檔案
- `gravity_photo.jpg` / `horizon_photo.jpg` — RGB 照片
- `gravity_depth_combined.jpg` — RGB + Depth 並排
- `gravity_pointcloud.ply` / `horizon_pointcloud.ply` — 3D 點雲
- `horizon_pointcloud.html` — 瀏覽器互動式 3D 查看器
- `horizon_depth_overlay.jpg` — 深度疊加照片對比
- `horizon_depth_on_photo.jpg` — 深度投射在照片上
- `horizon_comparison.jpg` — 四格總覽
- `horizon_3axis.jpg` — 三軸空間測量標線
- `horizon_measured.jpg` — 距離測量標線

---

## 下週待辦
- [ ] 設定個人 Discord DM Bot（額外加分）
- [ ] 嘗試 Gaze + Bot 雙向通訊
- [ ] 探索多攝影機協作

---

*日誌由 Claude Code 自動生成*
